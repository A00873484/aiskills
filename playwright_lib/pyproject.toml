[project]
name = "playwright_lib"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.40.0",
]

[project.optional-dependencies]
browser-use = ["browser-use>=0.12.1", "langchain-anthropic>=0.3.0"]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]
